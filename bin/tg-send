#!/usr/bin/env bash
# tg-send — deprecated name, kept so existing host-local scripts keep working.
# Delivery lives in bin/notify now, which routes over the active transport
# (OGMA_TRANSPORT) instead of assuming Telegram.
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/notify" "$@"
