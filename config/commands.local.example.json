{
  "_comment": [
    "TEMPLATE — copy to config/commands.local.json to add your own Telegram slash commands.",
    "The gateway merges these with its built-in core commands at runtime, so your additions",
    "survive `git pull` and never need editing the tracked source. The real file is gitignored.",
    "",
    "Each command maps a Telegram /cmd to an ogmactl subcommand. ogmactl (and your gitignored",
    "ogmactl.local) still gate execution, so listing a command here can't run anything ogmactl",
    "would otherwise refuse. Fields:",
    "  cmd      - the Telegram command name, without the slash. Telegram allows only [a-z0-9_], max 32.",
    "  run      - the ogmactl subcommand to execute (hyphens allowed, e.g. 'pihole-stats').",
    "  desc     - short text shown in the / menu and /help.",
    "  menu     - true to surface it in Telegram's / menu (false = works when typed, but hidden).",
    "  args     - optional usage hint shown in /help, e.g. '<dur>' or '<mac> <person>'.",
    "  guest    - true to let TELEGRAM_GUEST_USERS chats run it (default false = admins only).",
    "             Only mark read-only commands as guest.",
    "  confirm  - true to arm the command instead of running it: the user must send /confirm",
    "             within the confirm window (default 180s, OGMA_CONFIRM_TTL) or /cancel.",
    "             Use for anything destructive or disruptive.",
    "  validate - optional list of regexes, one per positional argument, checked by the gateway",
    "             BEFORE anything is executed. The list length is the maximum number of args;",
    "             a broken regex disables the whole command (fails closed).",
    "  min_args - with validate: how many of those args are required (default 0)."
  ],
  "commands": [
    {"cmd": "myhost",      "run": "myhost-status", "desc": "Example: a host-local status command", "menu": true, "guest": true},
    {"cmd": "myhost_thing", "run": "myhost-thing", "desc": "Example with a validated argument", "menu": false,
     "args": "<dur>", "validate": ["^[0-9]{1,4}[smh]?$"], "min_args": 1},
    {"cmd": "myhost_reboot", "run": "myhost-reboot", "desc": "Example of a protected command", "menu": false, "confirm": true}
  ]
}
